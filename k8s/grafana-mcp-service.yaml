apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp-service
  namespace: sentinel-mind
spec:
  selector:
    app: grafana-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
